import type { CompanySort } from "@/features/companies/types"

export const companySortLabels: Record<CompanySort, string> = {
  active_desc: "Active apps",
  name_asc: "Name",
  updated_desc: "Recently updated",
}
